<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/black" />
    <!-- Centered codeit wordmark via text is not supported in drawable;
         splash is handled by Flutter. This just provides a black background. -->
</layer-list>
